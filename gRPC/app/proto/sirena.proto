syntax="proto3";

option go_package="./proto";

package sirena_service;

service SirenaService{
    rpc SendEvents(Channel) returns (stream Event) {};
}

message Channel{
    string uuid=1;
}
message Event{
    uint32 priority=1;
    string message=2;
    bytes audio=3;
}